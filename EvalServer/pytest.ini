[pytest]
testpaths = tests
asyncio_mode = strict
markers =
    integration: real-API smoke test, opt-in via RUN_INTEGRATION_TESTS=1 and per-provider API keys.
filterwarnings =
    ignore::DeprecationWarning:pydantic.*
    ignore::DeprecationWarning:sqlalchemy.*
