import { text } from "../../../../themes/palette";
export const styles = {
  container: {
    textAlign: 'center', 
    padding: 30
  },
  titleText: {
    fontSize: 16,
    color: `${text.secondary}`,
    fontWeight: "bold",
    mb: 2
  },
  baseText: {
    color: `${text.secondary}`,
    fontSize: 13,
  }
}