import { text } from "../../themes/palette";
export const styles = {
  vwHeadingTitle: {
    color: `${text.primary}`,
    fontSize: 16,
    fontWeight: 600,
    marginBottom: 8,
  },
  vwSubHeadingTitle: {
    color: `${text.secondary}`,
    fontSize: 13,
  },
  reportButtonContainer: {
    display: "flex",
    justifyContent: "flex-end",
    alignItems: "center",
    mb: 2,
  },
};
